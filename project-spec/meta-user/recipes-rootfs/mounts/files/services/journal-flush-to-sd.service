[Unit]
Description=[ExAlta3] Flush runtime journal to SD
After=var-log.mount systemd-journald.service
Requires=var-log.mount

[Service]
Type=oneshot
ExecStart=/bin/journalctl --flush
ExecStart=/bin/journalctl --rotate

[Install]
WantedBy=multi-user.target